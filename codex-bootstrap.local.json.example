{
  "_comment": "Optional. Copy to codex-bootstrap.local.json (gitignored). `routingPath` entries runtime-read authority files. `taskEvidenceRoutes` can additionally inject line-numbered Markdown excerpts on each matching turn for both Claude and Codex. Missing files or unmatched excerpt terms fail closed. Override with AHR_CODEX_BOOTSTRAP_CONFIG.",
  "routingPath": "%USERPROFILE%\\agent_global_configs\\PROJECT_ROUTING.md",
  "taskEvidenceRoutes": []
}
